import React from "react";
import ReactDOM from "react-dom/client";


const root = ReactDOM.createRoot(document.getElementById('root'));

const header = () =>{
    return <h1>hello foodiis</h1>
};

root.render(header());